# chan.py 代码走读说明 —— 按阅读顺序的代码导读

> 本文档**不是**按模块平铺介绍，而是按**阅读代码的最佳顺序**组织。
> 建议按照这个顺序逐层阅读，每一层理解了再进入下一层。
> 每个阶段会说明：**先看哪个文件的哪个方法，再看哪个，以及为什么这个顺序**。

---

## 目录

- [阅读路线总览](#阅读路线总览)
- [第一阶段：建立概念地基（Common/ + 数据模型）](#第一阶段建立概念地基)
- [第二阶段：理解核心计算流水线（KLine + Combiner）](#第二阶段理解核心计算流水线)
- [第三阶段：从分形到笔（Bi/）](#第三阶段从分形到笔)
- [第四阶段：从笔到线段（Seg/）](#第四阶段从笔到线段)
- [第五阶段：中枢与背驰（ZS/）](#第五阶段中枢与背驰)
- [第六阶段：买卖点计算（BuySellPoint/）](#第六阶段买卖点计算)
- [第七阶段：理解主入口与配置（Chan.py + ChanConfig.py）](#第七阶段理解主入口与配置)
- [第八阶段：数据源与绘图（DataAPI/ + Plot/）](#第八阶段数据源与绘图)
- [第九阶段：技术指标（Math/）](#第九阶段技术指标)
- [第十阶段：实战演示（Debug/ + main.py）](#第十阶段实战演示)

---

## 阅读路线总览

```
第一阶段          第二阶段           第三阶段        第四阶段
Common/ ──────► KLine/ ──────► Bi/ ────────► Seg/
(枚举/时间/工具)  (K线单元/合并)  (笔的构建)     (线段算法)
                                                    │
第五阶段          第六阶段            第七阶段        ▼
ZS/ ◄──────── BuySellPoint/ ◄──── Chan.py +     Seg/
(中枢构建)       (买卖点计算)       ChanConfig.py  (特征序列)
                                    (主入口/配置)
                                                      │
第八阶段          第九阶段            第十阶段          ▼
DataAPI/ ────── Math/ ────────── Debug/ +       Plot/
(数据源)         (技术指标)         main.py        (绘图)
```

**核心依赖链**（按这个顺序理解）：

```
枚举/工具 → K线合并 → 分形 → 笔 → 特征序列 → 线段 → 中枢 → 买卖点 → 主入口/绘图
```

---

## 第一阶段：建立概念地基

> **目标**：理解项目中使用的基础类型、时间处理、缓存机制。这是阅读后续代码的前提。

### 阅读顺序

#### 1.1 先看 [Common/CEnum.py](Common/CEnum.py) —— 所有枚举定义

**为什么要先看这个**：这个文件定义了项目中所有枚举类型，是整个项目的"词典"。后续代码中会频繁出现 `KL_TYPE`、`BI_DIR`、`FX_TYPE`、`BSP_TYPE` 等，如果不先看这个，代码会完全看不懂。

**重点看**：
- `KL_TYPE`：K线级别（值越大级别越高，K_DAY=15）
- `KLINE_DIR`：K线方向（UP/DOWN/COMBINE/INCLUDED）
- `FX_TYPE`：分形类型（BOTTOM/TOP/UNKNOWN）
- `BI_DIR`：笔方向（UP/DOWN）
- `BI_TYPE`：笔类型（STRICT/SUB_VALUE/TIAOKONG_THRED 等）
- `BSP_TYPE`：买卖点类型（T1/T1P/T2/T2S/T3A/T3B）
- `MACD_ALGO`：背驰判断指标（PEAK/AREA/SLOPE/AMP 等）

**阅读时间**：约 10 分钟

---

#### 1.2 再看 [Common/CTime.py](Common/CTime.py) —— 时间类

**为什么要看这个**：缠论分析涉及多级别（日线/60分钟/15分钟等），级别之间的时间对齐是核心问题。`CTime` 的 `auto` 参数是关键设计。

**重点看**：
- `__init__` 中的 `auto` 参数：当 `auto=True` 且时间为 0:00 时，自动调整为 23:59。这确保了日线的时间 > 所有当天分钟线的时间
- `__gt__`/`__ge__`：基于时间戳的比较，用于 K 线排序

**阅读时间**：约 5 分钟

---

#### 1.3 再看 [Common/ChanException.py](Common/ChanException.py) —— 异常类

**为什么要看这个**：简单了解异常体系即可，后续遇到异常时能快速定位。

**阅读时间**：约 2 分钟

---

#### 1.4 再看 [Common/cache.py](Common/cache.py) —— 缓存装饰器

**为什么要看这个**：`@make_cache` 在 `Bi.py` 和 `KLine_Combiner.py` 中大量使用。理解它的工作原理（描述器协议 + 方法绑定）才能理解为什么某些计算结果会被缓存。

**重点看**：
- `__get__`：描述器协议，确保每个实例有独立的缓存字典
- `__call__`：首次调用执行原函数，后续调用直接返回缓存值

**阅读时间**：约 5 分钟

---

#### 1.5 最后看 [Common/func_util.py](Common/func_util.py) —— 工具函数

**为什么要看这个**：`has_overlap` 是中枢计算的核心函数，`kltype_lt_day` 用于判断是否是日内级别。

**重点看**：
- `has_overlap(l1, h1, l2, h2, equal)`：判断两个区间是否重叠，`equal=True` 时边界相等也算重叠
- `kltype_lt_day(_type)`：判断是否是分钟线级别

**阅读时间**：约 3 分钟

---

### 第一阶段小结

完成第一阶段后，你应该掌握了：
- 项目中所有枚举的含义（`BI_DIR`、`FX_TYPE`、`BSP_TYPE` 等）
- 时间类 `CTime` 的 `auto` 对齐机制
- `@make_cache` 缓存装饰器的工作原理
- `has_overlap` 区间重叠判断

---

## 第二阶段：理解核心计算流水线

> **目标**：理解 K 线数据如何从原始数据一步步变成缠论分析结果。
> **关键文件**：KLine_List.py 的 `add_single_klu` 和 `cal_seg_and_zs` 两个方法。

### 阅读顺序

#### 2.1 先看 [KLine/KLine_Unit.py](KLine/KLine_Unit.py) —— 单根K线

**为什么要先看这个**：这是整个计算流水线的**数据单元**。所有后续计算都基于它。

**重点看**：
- 属性：`time`、`open/close/high/low`、`sub_kl_list`（子级别K线）、`sup_kl`（父级别K线）、`macd/boll/rsi/kdj`（指标结果）
- `set_metric(metric_model_lst)`：将 K 线数据送入所有指标模型计算，结果存到 `self.macd` 等属性
- `add_children(child)` / `set_parent(parent)`：建立跨级别父子关系

**阅读时间**：约 8 分钟

---

#### 2.2 再看 [KLine/TradeInfo.py](KLine/TradeInfo.py) —— 交易信息

**为什么要看这个**：简单看一眼即可，存储成交量、成交额、换手率等扩展数据。

**阅读时间**：约 2 分钟

---

#### 2.3 再看 [Combiner/Combine_Item.py](Combiner/Combine_Item.py) —— 合并元素包装器

**为什么要看这个**：这是 `CKLine_Combiner` 能同时处理 K线/笔/线段 的关键。它用适配器模式统一了三种类型的接口。

**重点看**：
- `__init__`：根据 `isinstance(item, CBi)` 判断类型，提取统一的 `time_begin/time_end/high/low` 属性

**阅读时间**：约 3 分钟

---

#### 2.4 **重点** [Combiner/KLine_Combiner.py](Combiner/KLine_Combiner.py) —— 包含处理合并器

**为什么要重点看**：这是整个项目中**最重要的基类之一**。K 线包含处理、分形检测都在这里。`CKLine`（合并K线）和 `CEigen`（特征序列）都继承自它。

**阅读顺序**：
1. 先看 `test_combine(item, ...)`：判断两根 K 线的关系（包含/被包含/向上/向下）
2. 再看 `try_add(unit_kl, ...)`：尝试将新 K 线加入合并，如果需要合并就更新高低点
3. 最后看 `update_fx(_pre, _next, ...)`：检测顶底分型（中间元素的高点最高+低点最高=顶分型）

**缠论知识对照**：
- K 线包含处理：上升趋势取高高（高=max, 低=max），下降趋势取低低（高=min, 低=min）
- 顶分型：三根K线中，中间K线的高点最高，且中间K线的低点最高
- 底分型：三根K线中，中间K线的低点最低，且中间K线的高点最低

**阅读时间**：约 15 分钟

---

#### 2.5 再看 [KLine/KLine.py](KLine/KLine.py) —— 合并K线

**为什么要看这个**：继承自 `CKLine_Combiner`，增加了分型合法性检查和跨级别子K线获取。

**重点看**：
- `check_fx_valid(item2, method, for_virtual)`：检查顶底分型是否合法。支持 strict/loss/half/totally 四种方法
- `GetSubKLC()`：获取子级别对应的合并K线列表

**阅读时间**：约 8 分钟

---

#### 2.6 **重点** [KLine/KLine_List.py](KLine/KLine_List.py) —— K线列表容器

**为什么要重点看**：这是**每个级别的总容器**，是整个计算流水线的调度中心。理解了这个文件，就理解了整个项目的运行流程。

**阅读顺序**：
1. **先看 `add_single_klu(klu)`**：这是核心入口方法。每来一根新 K 线，这里的流程是：
   
   ```
   1. 计算指标 set_metric()
   2. 尝试合并K线 → try_add()
   3. 检测分形 → update_fx()
   4. 更新笔 → bi_list.update_bi()
   5. 如果是步进模式 → cal_seg_and_zs() 实时重算
   ```
2. **再看 `cal_seg_and_zs()`**：这是批量模式的最终计算方法。流程是：
   
   ```
   1. 补全虚笔 → try_add_virtual_bi()
   2. 计算线段 → cal_seg()
   3. 计算中枢 → zs_list.cal_bi_zs()
   4. 中枢分配到线段 → update_zs_in_seg()
   5. 计算线段的线段 → segseg_list
   6. 计算线段中枢 → segzs_list
   7. 计算买卖点 → bs_point_lst.cal()
   ```

**阅读时间**：约 15 分钟

---

### 第二阶段小结

完成第二阶段后，你应该掌握了：
- CKLine_Unit（单根K线）的数据结构
- CKLine_Combiner 的包含处理逻辑（test_combine → try_add → update_fx）
- CKLine_List 的完整计算流水线（add_single_klu → cal_seg_and_zs）
- 数据流：原始K线 → 合并K线 → 分形 → 笔 → 线段 → 中枢 → 买卖点

---

## 第三阶段：从分形到笔

> **目标**：理解分形如何变成笔，什么是虚笔，笔的确定条件是什么。

### 阅读顺序

#### 3.1 先看 [Bi/BiConfig.py](Bi/BiConfig.py) —— 笔的配置

**为什么要先看这个**：了解笔的配置参数，后续代码会用到这些参数。

**重点看**：
- `is_strict`：严格笔要求顶底分型间至少隔 4 根合并K线
- `bi_fx_check`：分型检查方法（strict/loss/half/totally）
- `gap_as_kl`：缺口是否算一根K线
- `bi_end_is_peak`：笔的尾部是否是整笔中最低/最高点
- `bi_allow_sub_peak`：是否允许次高点成笔

**阅读时间**：约 3 分钟

---

#### 3.2 **重点** [Bi/Bi.py](Bi/Bi.py) —— 笔类

**为什么要重点看**：笔是缠论分析的核心单元，线段和中枢都基于笔。

**阅读顺序**：
1. 先看 `__init__` 和属性：`begin_klc/end_klc`（起止合并K线）、`dir`（方向）、`is_sure`（是否确定）
2. 再看 `get_begin_val()/get_end_val()`：上升笔起点=底分型低点、终点=顶分型高点；下降笔反之
3. 再看 `_high()/_low()/_mid()`：笔的最高/最低/中位价，用于中枢计算
4. 最后看 `cal_macd_metric(macd_algo, is_reverse)`：计算笔的 MACD 指标，这是背驰判断的基础

**缠论知识**：
- 上升笔：底分型 → 顶分型，起点=底分型低点，终点=顶分型高点
- 下降笔：顶分型 → 底分型，起点=顶分型高点，终点=底分型低点

**阅读时间**：约 10 分钟

---

#### 3.3 **重点** [Bi/BiList.py](Bi/BiList.py) —— 笔列表管理

**为什么要重点看**：这是笔的构建和管理逻辑，是整个系统中逻辑最复杂的部分之一。

**阅读顺序**：
1. **先看 `update_bi(klc, last_klc, cal_virtual)`**：核心入口。每次新增合并K线时调用
2. **再看 `update_bi_sure(klc)`**：更新确定笔。根据新K线的分型类型，决定创建新笔/更新笔尾/更新峰值
3. **再看 `try_create_first_bi(klc)`**：第一笔的创建（需要等至少两个不同分型出现）
4. **再看 `can_make_bi(klc, last_end, for_virtual)`**：判断是否满足成笔条件
5. **再看 `try_add_virtual_bi(klc, need_del_end)`**：虚笔的创建和更新
6. 最后看 `add_new_bi(pre_klc, cur_klc, is_sure)`：创建新笔并加入列表

**关键概念**：
- **虚笔（is_sure=False）**：笔的终点还在延伸（如果后续K线创新高/新低，笔的终点会移动）
- **实笔（is_sure=True）**：笔的终点已确定，不会再变

**阅读时间**：约 20 分钟

---

### 第三阶段小结

完成第三阶段后，你应该掌握了：
- 笔的定义（连接顶底分型，至少间隔4根合并K线）
- 虚笔 vs 实笔的区别
- can_make_bi 的成笔条件判断
- 笔的 MACD 指标计算方法

---

## 第四阶段：从笔到线段

> **目标**：理解线段算法，尤其是特征序列法（默认算法）。
> **这是整个项目中最复杂的部分**，需要耐心阅读。

### 阅读顺序

#### 4.1 先看 [Seg/SegConfig.py](Seg/SegConfig.py) —— 线段配置

**阅读时间**：约 1 分钟

---

#### 4.2 再看 [Seg/Seg.py](Seg/Seg.py) —— 线段类

**为什么要先看这个**：了解线段的数据结构，为理解线段算法做准备。

**重点看**：
- `start_bi/end_bi`：线段的起始笔和终止笔
- `dir`：线段方向
- `eigen_fx`：特征序列分型（线段转线段时使用）
- `update_bi_list(bi_lst, idx1, idx2)`：更新线段内的笔列表，并计算趋势线
- `get_first_multi_bi_zs()/get_final_multi_bi_zs()`：获取线段内的中枢

**阅读时间**：约 8 分钟

---

#### 4.3 再看 [Seg/Eigen.py](Seg/Eigen.py) —— 特征序列元素

**为什么要看这个**：特征序列法是默认线段算法，必须先理解特征序列元素。

**重点看**：
- 继承自 `CKLine_Combiner[CBi]`，将笔当作 K 线进行包含处理
- `gap` 属性：特征序列元素之间是否有跳空
- `update_fx`：检测特征序列分型，同时判断是否有跳空

**缠论知识**：
- 上升线段中，下降笔构成特征序列（因为它们是"反向"的）
- 对特征序列进行包含处理，类似 K 线合并
- 在特征序列中找顶底分型来确定线段端点

**阅读时间**：约 5 分钟

---

#### 4.4 **重点** [Seg/EigenFX.py](Seg/EigenFX.py) —— 特征序列分型

**为什么要重点看**：这是特征序列线段算法的核心。管理特征序列的三元素（类似 K 线分型的三元素），处理跳空和确认。

**阅读顺序**：
1. **先看 `add(bi)`**：核心入口。根据当前所处的阶段（第一/二/三元素），调用对应的处理方法
2. **再看 `treat_first_ele(bi)`**：创建第一个 CEigen 元素
3. **再看 `treat_second_ele(bi)`**：尝试与第一元素合并，不能合并则创建第二元素
4. **再看 `treat_third_ele(bi)`**：尝试与第二元素合并，形成分型后检查是否实际突破
5. **再看 `can_be_end(bi_lst)`**：判断特征序列分型是否可以作为线段终点
   - 无跳空：分型出现即确认
   - 有跳空：需要等待反向分型确认
6. **再看 `find_revert_fx(bi_lst, begin_idx, ...)`**：跳空时寻找反向分型确认
7. 最后看 `reset()`：分型不成立时重置

**缠论知识**：
- 特征序列分型第二元素之间有**跳空**（gap）→ 需要等待反向分型确认
- 无跳空 → 分型出现即确认线段结束
- 这是缠论线段划分中最核心的规则

**阅读时间**：约 25 分钟（这是最复杂的文件）

---

#### 4.5 **重点** [Seg/SegListComm.py](Seg/SegListComm.py) —— 线段列表通用父类

**为什么要看这个**：所有线段算法（chan/def/dyh）都继承自它。它处理的是"线段列表中不确定的部分"（虚段）。

**重点看**：
- `try_add_new_seg(bi_lst, end_bi_idx, ...)`：尝试添加新线段，处理首段拆分
- `collect_first_seg(bi_lst)`：收集第一段（开头不确定的虚段）
- `collect_left_seg(bi_lst)`：收集剩余未归入确定线段的笔为虚段
- `collect_left_seg_peak_method(...)`：PEAK 方法找新极值点形成虚段
- `left_bi_break(bi_lst)`：判断最后一个确定线段后的笔是否突破了线段

**阅读时间**：约 15 分钟

---

#### 4.6 **重点** [Seg/SegListChan.py](Seg/SegListChan.py) —— 特征序列线段算法

**为什么要重点看**：这是默认的线段算法，基于缠论原文的特征序列方法。

**阅读顺序**：
1. **先看 `update(bi_lst)`**：核心入口。1) 重置不确定段 → 2) 从上次确定段之后重新计算确定段 → 3) 收集剩余虚段
2. **再看 `do_init()`**：删除末尾不确定的线段
3. **再看 `cal_seg_sure(bi_lst, begin_idx)`**：核心方法。创建两个 `CEigenFX`（上升和下降特征序列），遍历笔，寻找特征序列分型
4. 最后看 `treat_fx_eigen(fx_eigen, bi_lst)`：处理找到的特征序列分型，判断是否可以作为线段终点

**阅读时间**：约 15 分钟

---

#### 4.7 可选 [Seg/SegListDef.py](Seg/SegListDef.py) 和 [Seg/SegListDYH.py](Seg/SegListDYH.py)

这两个是已弃用的线段算法，可以快速浏览或不看。

**阅读时间**：约 5 分钟

---

### 第四阶段小结

完成第四阶段后，你应该掌握了：
- 特征序列的概念（上升线段中的下降笔构成特征序列）
- 特征序列包含处理（类似 K 线合并）
- 特征序列分型的形成（三元素）
- 跳空与确认规则（有gap需反向分型确认）
- 虚段处理（peak方法和all方法）

---

## 第五阶段：中枢与背驰

> **目标**：理解中枢的构建、合并，以及背驰的判断。

### 阅读顺序

#### 5.1 先看 [ZS/ZSConfig.py](ZS/ZSConfig.py) —— 中枢配置

**阅读时间**：约 1 分钟

---

#### 5.2 **重点** [ZS/ZS.py](ZS/ZS.py) —— 中枢类

**为什么要重点看**：中枢是缠论中最重要的概念，是买卖点判断的核心依据。

**阅读顺序**：
1. 先看 `__init__` 和属性：`low/high`（中枢区间）、`begin_bi/end_bi`（中枢内的笔）、`bi_in/bi_out`（进入/离开中枢的笔）
2. 再看 `update_zs_range(lst)`：更新中枢区间，low=max(各笔低点), high=min(各笔高点)
3. **重点看 `combine(zs2, combine_mode)`**：尝试合并两个中枢。zs模式要求区间重叠，peak模式要求K线重叠
4. **重点看 `is_divergence(config, out_bi)`**：判断是否背驰。比较 bi_in 和 bi_out 的 MACD 指标
5. 再看 `end_bi_break(end_bi)`：判断离开中枢的笔是否突破了中枢
6. 最后看 `is_one_bi_zs()`：是否是一笔中枢

**缠论知识**：
- 中枢区间 = [max(各笔低点), min(各笔高点)]，即"重叠区间"
- 中枢合并：相邻中枢区间有重叠时可以合并
- 背驰：离开中枢的笔 MACD 弱于进入中枢的笔

**阅读时间**：约 12 分钟

---

#### 5.3 **重点** [ZS/ZSList.py](ZS/ZSList.py) —— 中枢列表管理

**为什么要重点看**：管理中枢的构建、合并、分配。

**阅读顺序**：
1. **先看 `cal_bi_zs(bi_lst, seg_lst)`**：核心入口。根据线段列表计算中枢，支持 normal/over_seg/auto 三种算法
2. 再看 `try_construct_zs(lst, is_sure, zs_algo)`：尝试从笔列表构造中枢
3. 再看 `try_combine()`：尝试合并相邻中枢
4. 再看 `add_zs_from_bi_range(seg_bi_lst, seg_dir, seg_is_sure)`：从一个线段的笔范围中计算中枢

**缠论知识**：
- 段内中枢（normal）：中枢不跨段，每个线段内部独立计算
- 跨段中枢（over_seg）：允许中枢跨越线段边界
- 中枢需要至少3笔重叠（下上下或上下上）

**阅读时间**：约 15 分钟

---

### 第五阶段小结

完成第五阶段后，你应该掌握了：
- 中枢的形成条件（三笔重叠）
- 中枢区间的计算（low=max(低点), high=min(高点)）
- 中枢合并规则（zs/peak 模式）
- 背驰判断（进入笔 vs 离开笔的 MACD 比较）

---

## 第六阶段：买卖点计算

> **目标**：理解缠论三类买卖点的计算逻辑。

### 阅读顺序

#### 6.1 先看 [BuySellPoint/BSPointConfig.py](BuySellPoint/BSPointConfig.py) —— 买卖点配置

**重点看**：
- `divergence_rate`：背驰率阈值（默认 0.9）
- `bs_type`：需要计算的买卖点类型
- `macd_algo`：背驰计算指标算法

**阅读时间**：约 3 分钟

---

#### 6.2 再看 [BuySellPoint/BS_Point.py](BuySellPoint/BS_Point.py) —— 买卖点类

**重点看**：
- `type`：买卖点类型列表（一个位置可能同时是多种类型）
- `relate_bsp1`：关联的一类买卖点
- `features`：特征字典

**阅读时间**：约 3 分钟

---

#### 6.3 **重点** [BuySellPoint/BSPointList.py](BuySellPoint/BSPointList.py) —— 买卖点列表计算

**为什么要重点看**：这是买卖点计算的核心，包含所有类型买卖点的计算逻辑。

**阅读顺序**：
1. **先看 `cal(bi_list, seg_list)`**：核心入口。依次计算一类→二类→三类买卖点
2. **再看 `cal_single_bs1point(seg, bi_list)`**：计算单个线段的一类买卖点
   - 判断逻辑：1) 中枢数是否满足 → 2) 离开中枢的笔是否突破中枢 → 3) 是否背驰 → 4) 是否极值点
3. **再看 `treat_bsp1(seg, BSP_CONF, is_target_bsp)`**：处理标准背驰一类买卖点
4. **再看 `treat_pz_bsp1(seg, BSP_CONF, bi_list, is_target_bsp)`**：处理盘整背驰一类（T1P）
5. **再看 `cal_seg_bs2point(seg_list, bi_list)`**：计算二类买卖点（一类之后的第一笔回撤）
6. **再看 `cal_seg_bs3point(seg_list, bi_list)`**：计算三类买卖点（离开中枢后回抽不进入中枢）

**缠论知识**：
- **一类买卖点（T1）**：趋势背驰点。离开中枢的笔突破中枢，且相对于进入中枢的笔发生背驰
- **一类盘整背驰（T1P）**：没有完整中枢，但相邻两笔同向且背驰
- **二类买卖点（T2）**：一类之后的第一笔回撤，回撤不超阈值
- **类二买卖点（T2S）**：中枢内同侧的其他买卖点
- **三类买卖点（T3A/T3B）**：离开中枢后回抽不进入中枢
  - T3A：中枢在一类之后
  - T3B：中枢在一类之前

**阅读时间**：约 20 分钟

---

### 第六阶段小结

完成第六阶段后，你应该掌握了：
- 三类买卖点的定义和判断条件
- 背驰在买卖点判断中的作用
- 买卖点之间的关联关系（二类/三类跟随一类）

---

## 第七阶段：理解主入口与配置

> **目标**：理解整个系统是如何被调度和配置的。

### 阅读顺序

#### 7.1 先看 [ChanConfig.py](ChanConfig.py) —— 配置系统

**为什么要先看这个**：了解所有可配置参数，以及配置校验机制。

**重点看**：
- `CChanConfig.__init__`：所有配置参数的默认值
- `GetMetricModel()`：创建所有技术指标模型实例
- `set_bsp_config(conf)`：买卖点配置解析，支持 `-buy`/`-sell`/`-seg` 后缀
- `ConfigWithCheck`：配置校验器，消费后检查是否有未识别的 key

**阅读时间**：约 10 分钟

---

#### 7.2 **重点** [Chan.py](Chan.py) —— 主入口类

**为什么要重点看**：这是整个项目的**门面（Facade）**，所有功能都从这里开始。

**阅读顺序**：
1. **先看 `__init__`**：了解初始化参数和流程
2. **重点看 `load(step)`**：主加载方法。流程：获取数据源 → 初始化迭代器 → 递归加载 → 最终计算
3. **重点看 `load_iterator(lv_idx, parent_klu, step)`**：核心递归方法。从最高级别向最低级别递归加载 K 线
   - 理解缓存机制：子 K 线时间超过父 K 线时间时，缓存到 `klu_cache`，等待父 K 线前进
   - 理解父子关系：`set_klu_parent_relation` 建立跨级别链接
4. 再看 `step_load()`：步进模式（生成器），每根 K 线 yield 一个快照
5. 再看 `trigger_load(inp)`：外部喂 K 线模式
6. 再看 `__deepcopy__(memo)`：自定义深拷贝，手动重建跨级别指针
7. 最后看 `chan_dump_pickle`/`chan_load_pickle`：序列化/反序列化

**重点理解递归加载逻辑**：
```
load_iterator(lv=0, parent=None)   # 最高级别（如日线）
  ├─ 读取一根日线K线
  ├─ add_new_kl() → 加入日线列表（触发包含处理、分形、笔）
  ├─ load_iterator(lv=1, parent=日线K线)  # 递归加载子级别
  │   ├─ 读取60分钟K线（在日线K线时间范围内）
  │   │   ├─ 如果60M时间 > 日线时间 → 缓存到 klu_cache，等待下根日线
  │   │   └─ 否则 → 加入60M列表，继续递归子级别
  │   └─ 返回
  ├─ 如果是步进模式 → yield self（产生快照）
  └─ 继续下一根日线
```

**阅读时间**：约 25 分钟

---

### 第七阶段小结

完成第七阶段后，你应该掌握了：
- 整个系统的初始化流程
- 递归加载多级别 K 线的逻辑
- 时间对齐机制（缓存超出父K线时间的子K线）
- 两种运行模式（批量/步进）
- 序列化/反序列化机制

---

## 第八阶段：数据源与绘图

> **目标**：了解数据如何获取，以及分析结果如何可视化。
> 这两个模块相对独立，可以在理解核心逻辑后再看。

### 阅读顺序

#### 8.1 先看 [DataAPI/CommonStockAPI.py](DataAPI/CommonStockAPI.py) —— 数据源抽象基类

**重点看**：
- `get_kl_data()`：抽象方法，返回 K 线数据的生成器
- `SetBasciInfo()`：设置股票名称等信息
- `do_init()`/`do_close()`：类方法，初始化和清理

**阅读时间**：约 3 分钟

---

#### 8.2 再看 [DataAPI/BaoStockAPI.py](DataAPI/BaoStockAPI.py) —— 默认数据源

**重点看**：
- `get_kl_data()`：从 BaoStock 获取 K 线数据
- `__convert_type()`：级别类型转换

**阅读时间**：约 5 分钟

---

#### 8.3 可选 AkshareAPI.py / csvAPI.py / ccxt.py

其他数据源实现，快速浏览即可。

**阅读时间**：约 5 分钟

---

#### 8.4 先看 [Plot/PlotMeta.py](Plot/PlotMeta.py) —— 绘图元数据

**为什么要先看这个**：将分析结果转换为绘图用的坐标数据。

**重点看**：
- `CChanPlotMeta.__init__`：将所有缠论元素转换为元数据
- `klu_iter()`：生成器，遍历所有原始K线

**阅读时间**：约 5 分钟

---

#### 8.5 再看 [Plot/PlotDriver.py](Plot/PlotDriver.py) —— 绘图驱动

**为什么要看这个**：了解如何将所有缠论元素可视化。

**重点看**：
- `DrawElement(plot_config, ...)`：根据配置逐一绘制
- `draw_klu()`：绘制 K 线（阴阳烛）
- `draw_bi()`：绘制笔
- `draw_seg()`：绘制线段
- `draw_zs()`：绘制中枢
- `draw_bs_point()`：绘制买卖点
- `draw_macd()`：绘制 MACD

**阅读时间**：约 10 分钟

---

#### 8.6 可选 [Plot/AnimatePlotDriver.py](Plot/AnimatePlotDriver.py) —— 动画绘图

步进模式动画，用于 Jupyter Notebook。

**阅读时间**：约 3 分钟

---

## 第九阶段：技术指标

> **目标**：了解各个技术指标的计算方法。
> 这些模块相对独立，可以按需阅读。

### 阅读顺序

#### 9.1 [Math/MACD.py](Math/MACD.py) —— MACD 指标

**最重要**：MACD 是背驰判断的核心指标。

**重点看**：
- `add(value)`：EMA 计算公式

**阅读时间**：约 5 分钟

---

#### 9.2 其余指标（按需阅读）

| 文件 | 指标 | 用途 | 阅读时间 |
|------|------|------|---------|
| [Math/BOLL.py](Math/BOLL.py) | 布林线 | 辅助判断波动率 | 3 分钟 |
| [Math/RSI.py](Math/RSI.py) | RSI | 辅助判断超买/超卖 | 3 分钟 |
| [Math/KDJ.py](Math/KDJ.py) | KDJ | 辅助判断超买/超卖 | 3 分钟 |
| [Math/TrendModel.py](Math/TrendModel.py) | 趋势模型 | 均线/通道 | 3 分钟 |
| [Math/TrendLine.py](Math/TrendLine.py) | 趋势线 | 支撑/阻力线 | 3 分钟 |
| [Math/Demark.py](Math/Demark.py) | 德马克 | 趋势衰竭识别 | 5 分钟 |

---

## 第十阶段：实战演示

> **目标**：通过运行示例代码，验证你的理解。

### 阅读顺序

#### 10.1 先看 [main.py](main.py) —— 基础演示

**重点看**：
- 配置参数的含义
- `CChan` 的创建和使用
- `CPlotDriver` 的绘图流程

**建议**：直接运行 `python main.py`，对照生成的图片理解各个元素。

**阅读时间**：约 5 分钟

---

#### 10.2 再看 [Debug/strategy_demo.py](Debug/strategy_demo.py) —— 步进模式策略

**重点看**：
- `chan.step_load()` 的使用方式
- 如何在步进模式下实现交易策略

**阅读时间**：约 5 分钟

---

#### 10.3 再看 [Debug/strategy_demo2.py](Debug/strategy_demo2.py) —— 外部喂K线模式

**重点看**：
- `trigger_load()` 的使用方式
- 如何从外部逐根推送 K 线

**阅读时间**：约 3 分钟

---

#### 10.4 可选 strategy_demo3.py ~ strategy_demo6.py

- **demo3**：小级别K线合成大级别（K线合成逻辑）
- **demo4**：多级别时间对齐技巧
- **demo5**：ML 特征生成 + XGBoost 训练
- **demo6**：实盘模型预测

**阅读时间**：共约 15 分钟

---

## 附录：按阅读时间估算的总览

| 阶段 | 内容 | 预计时间 |
|------|------|---------|
| 第一阶段 | Common/ 基础 | 25 分钟 |
| 第二阶段 | KLine + Combiner（核心流水线） | 50 分钟 |
| 第三阶段 | Bi/（笔） | 33 分钟 |
| 第四阶段 | Seg/（线段，最复杂） | 69 分钟 |
| 第五阶段 | ZS/（中枢与背驰） | 28 分钟 |
| 第六阶段 | BuySellPoint/（买卖点） | 26 分钟 |
| 第七阶段 | Chan.py + ChanConfig.py（主入口） | 35 分钟 |
| 第八阶段 | DataAPI + Plot（数据源与绘图） | 31 分钟 |
| 第九阶段 | Math/（技术指标） | 25 分钟 |
| 第十阶段 | Debug + main.py（实战） | 28 分钟 |
| **总计** | | **约 5.5 小时** |

> **建议**：分 2-3 天阅读。第一天看第一~三阶段（打好基础），第二天看第四~六阶段（最复杂的部分），第三天看第七~十阶段（收尾和实战）。

---

## 附录：关键概念速查

| 概念 | 定义 | 关键文件 |
|------|------|---------|
| 包含处理 | 相邻K线包含时合并 | KLine_Combiner.py |
| 分形 | 三根K线的顶/底形态 | KLine.py → check_fx_valid |
| 笔 | 连接顶底分型 | Bi.py + BiList.py |
| 虚笔 | 终点还在延伸的笔 | BiList.py → try_add_virtual_bi |
| 特征序列 | 线段中反向笔的序列 | Eigen.py |
| 特征序列分型 | 特征序列的顶底分型 | EigenFX.py |
| 线段 | 至少3笔组成的更大级别结构 | SegListChan.py |
| 中枢 | 三笔重叠区间 | ZS.py |
| 背驰 | 价格新高低但MACD未确认 | ZS.py → is_divergence |
| 一类买卖点 | 趋势背驰转折点 | BSPointList.py → treat_bsp1 |
| 二类买卖点 | 一类后的回撤 | BSPointList.py → treat_bsp2 |
| 三类买卖点 | 离开中枢后回抽不进入 | BSPointList.py → treat_bsp3_after/before |