# chan.py 阅读文档 —— 缠论 Python 实现快速上手指南

> 本文档面向初学者，从零开始讲解 chan.py 项目的架构、模块调用关系、每个方法的作用，以及方法背后的缠论知识。

---

## 目录

- [第一部分：项目概述](#第一部分项目概述)
- [第二部分：缠论核心概念速览](#第二部分缠论核心概念速览)
- [第三部分：项目整体架构与调用关系](#第三部分项目整体架构与调用关系)
- [第四部分：模块与方法级别拆解](#第四部分模块与方法级别拆解)
  - [4.1 Common/ — 公共基础设施](#41-common--公共基础设施)
  - [4.2 KLine/ — K线模块](#42-kline--k线模块)
  - [4.3 Combiner/ — 合并器模块](#43-combiner--合并器模块)
  - [4.4 Bi/ — 笔模块](#44-bi--笔模块)
  - [4.5 Seg/ — 线段模块](#45-seg--线段模块)
  - [4.6 ZS/ — 中枢模块](#46-zs--中枢模块)
  - [4.7 BuySellPoint/ — 买卖点模块](#47-buysellpoint--买卖点模块)
  - [4.8 Math/ — 技术指标模块](#48-math--技术指标模块)
  - [4.9 DataAPI/ — 数据源模块](#49-dataapi--数据源模块)
  - [4.10 Plot/ — 绘图模块](#410-plot--绘图模块)
  - [4.11 Chan.py — 主入口类](#411-chanpy--主入口类)
  - [4.12 ChanConfig.py — 配置系统](#412-chanconfigpy--配置系统)
  - [4.13 ChanModel/ — 模型特征模块](#413-chanmodel--模型特征模块)
- [第五部分：完整计算流水线详解](#第五部分完整计算流水线详解)
- [第六部分：实战示例](#第六部分实战示例)

---

## 第一部分：项目概述

### 1.1 什么是 chan.py

**chan.py** 是缠中说禅（缠论）技术分析理论的 Python 实现。作者是 **Vespa314**，MIT 协议开源。

核心能力：**输入股票 K 线数据 → 输出缠论分析结果**（分形、笔、线段、中枢、买卖点）。

本项目是 **fork 版本**，原始仓库地址：https://github.com/Vespa314/chan.py

### 1.2 开源版与完整版

- **开源版**（本仓库）：约 5300 行，包含缠论静态计算引擎，可计算笔、线段、中枢、买卖点，支持画图
- **完整版**（私有）：约 22000 行，额外包含策略类、交易引擎、ML 特征/模型、AutoML 框架、数据库对接等

### 1.3 环境要求

- **Python >= 3.11**（计算密集型，3.11 相比 3.8.5 有约 16% 性能提升）
- 安装依赖：`pip install -r Script/requirements.txt`

### 1.4 快速体验

```bash
python main.py
```

---

## 第二部分：缠论核心概念速览

在阅读代码之前，先理解缠论的基本概念，否则代码会很难理解。

### 2.1 K线

股票价格在固定时间周期内的 OHLC 数据（Open/High/Low/Close）。一根 K 线有开盘价、收盘价、最高价、最低价。

### 2.2 K线包含处理（合并K线）

缠论中，相邻两根 K 线如果一根的价格区间完全包含另一根，需要进行合并处理。

**规则：**
- 上升趋势中：取高点的高者、低点的高者（高高）
- 下降趋势中：取高点的低者、低点的低者（低低）

**举例：**
```
# 上升趋势中，K线2包含K线3
K线1: 高=10, 低=8  (向上)
K线2: 高=12, 低=7  (包含K线3)
K线3: 高=11, 低=7.5
合并后: 高=max(12,11)=12, 低=max(7,7.5)=7.5
```

代码中 `CKLine_Combiner` 类实现了这个逻辑。

### 2.3 分形（顶分型/底分型）

由三根连续的合并 K 线组成：

- **顶分型**：中间 K 线的高点最高，且中间 K 线的低点最高（三根 K 线中高点最高、低点也最高）
- **底分型**：中间 K 线的低点最低，且中间 K 线的高点最低（三根 K 线中低点最低、高点也最低）

**举例：**
```
顶分型：    A: 高=10, 低=8
            B: 高=12, 低=7.5   ← 中间这根高最高
            C: 高=11, 低=9

底分型：    A: 高=10, 低=8
            B: 高=9,  低=6     ← 中间这根低最低
            C: 高=11, 低=7
```

代码中 `CKLine_Combiner.update_fx()` 方法检测分形。

### 2.4 笔

笔是缠论中最基本的走势单元。**一笔连接一个顶分型和一个底分型**（或反过来）。

**条件：**
- 顶分型和底分型之间至少间隔 4 根合并 K 线（严格笔）
- 顶分型的最高点必须高于底分型的最低点（上升笔）
- 底分型的最低点必须低于顶分型的最高点（下降笔）

**举例：**
```
上升笔：底分型 → 中间若干K线（至少3根） → 顶分型
        (底分型低点)  →  (顶分型高点)
```

代码中 `CBi` 类表示一笔，`CBiList` 类管理所有笔。

### 2.5 线段

线段由至少 3 笔组成，方向相同（连续上升笔或连续下降笔）。

**特征序列法**（本项目默认算法）：
- 上升线段由下降笔序列组成（这些下降笔称为特征序列）
- 对特征序列进行包含处理后，找顶底分型
- 如果特征序列分型第二元素之间有跳空，需要等待反向分型确认

代码中 `CSegListChan` 类实现了特征序列线段算法。

### 2.6 中枢

中枢是缠论中最重要的概念。**至少连续 3 笔**（下上下或上下上）重叠的价格区间。

- 上升线段中的中枢：由下上下三笔构成
- 下降线段中的中枢：由上下上三笔构成
- 中枢区间 = [max(三笔的低点), min(三笔的高点)]

**举例：**
```
上升线段中的中枢：
  笔1: 下降笔，从高10到低7
  笔2: 上升笔，从低7到高9
  笔3: 下降笔，从高9到低8
  中枢区间: [max(7,7,8), min(10,9,9)] = [8, 9]
```

代码中 `CZS` 类表示一个中枢，`CZSList` 类管理所有中枢。

### 2.7 买卖点

缠论定义了三种买卖点：

- **一类买卖点（T1）**：离开中枢的笔相对于进入中枢的笔发生背驰。一买是下跌趋势的终点，一卖是上涨趋势的终点
- **二类买卖点（T2）**：一类买卖点之后的第一笔回调（回撤到 A 的比例不超阈值）
- **三类买卖点（T3A/T3B）**：中枢之后离开中枢后，回抽不进入中枢的确认点
- **类二买卖点（T2S）**：中枢内与二类买卖点同侧的后续买卖点

代码中 `CBSPointList` 类计算所有买卖点。

### 2.8 背驰

背驰是指价格创新高/新低，但对应的 MACD 指标没有同步创新高/新低，说明趋势力度减弱。

代码中 `CZS.is_divergence()` 方法判断背驰。

---

## 第三部分：项目整体架构与调用关系

### 3.1 核心计算流水线

```
CChan (Chan.py) ← 门面/Facade
  │
  ├─ GetStockAPI() → DataAPI 数据源选择
  │
  ├─ load() → load_iterator()
  │   │
  │   ├─ 读 K 线 → CKLine_Unit (单根K线)
  │   │     └─ set_metric() → Math/ 指标计算
  │   │
  │   ├─ CKLine_Combiner → CKLine (合并K线)
  │   │     └─ update_fx() → 分形检测
  │   │
  │   ├─ CBiList.update_bi() → CBi (笔)
  │   │
  │   └─ (step模式) 每根K线 → cal_seg_and_zs()
  │         ├─ CSegListChan → CSeg (线段)
  │         ├─ CZSList.cal_bi_zs() → CZS (中枢)
  │         └─ CBSPointList.cal() → CBS_Point (买卖点)
  │
  └─ (批量模式) → cal_seg_and_zs() 一次性完成
```

### 3.2 多级别递归关系

`CChan.load_iterator()` 从最高级别向最低级别递归：

```
年线 → 月线 → 周线 → 日线 → 60M → 30M → ... → 1M
  │       │       │
  └─父K线─┘       │
          └───父K线─┘  (通过 sup_kl/sub_kl_list 链接)
```

每个父 K 线（如日线）包含其下的子 K 线列表（如 60M 线），支持**区间套**策略——在父 K 线下查看子级别的买卖点信号。

### 3.3 两种运行模式

| 模式 | `trigger_step` | 行为 |
|------|---------------|------|
| **批量模式** | `False`（默认） | 加载全部数据后一次性计算所有缠论元素 |
| **步进模式** | `True` | `CChan` 变为生成器，每根新 K 线产生一个快照，用于回测和动画 |

### 3.4 数据流调用图

```
DataAPI (数据源)
  ↓ CKLine_Unit
CKLine_Combiner (K线合并)
  ↓ CKLine (含分形)
CBiList (笔列表)
  ↓ CBi
CSegListChan (线段列表, 特征序列法)
  ↓ CSeg
CZSList (中枢列表)
  ↓ CZS
CBSPointList (买卖点列表)
  ↓ CBS_Point
```

### 3.5 核心类继承关系

```
CKLine_Combiner[T] (泛型，包含处理合并逻辑)
  ├── CKLine (T = CKLine_Unit) 合并K线
  └── CEigen (T = CBi) 特征序列元素

CSegListComm[SUB_LINE_TYPE] (泛型，线段列表通用父类)
  ├── CSegListChan (特征序列法)
  ├── CSegListDef (线段破坏法，已弃用)
  └── CSegListDYH (1+1终结法，已弃用)

CCommonStockApi (抽象数据源)
  ├── CBaoStock (BaoStock)
  ├── CCXT (加密货币)
  ├── CSV_API (CSV文件)
  └── CAkshare (Akshare)
```

---

## 第四部分：模块与方法级别拆解

### 4.1 Common/ — 公共基础设施

#### 4.1.1 CEnum.py — 所有枚举定义

定义了项目中所有枚举类型，是理解代码的"词典"。

| 枚举类 | 用途 | 取值 |
|--------|------|------|
| `KL_TYPE` | K线级别 | `K_1S`(1秒) → `K_YEAR`(年)，值越大级别越高 |
| `KLINE_DIR` | K线方向 | `UP`, `DOWN`, `COMBINE`(合并中), `INCLUDED`(被包含) |
| `FX_TYPE` | 分形类型 | `BOTTOM`(底分型), `TOP`(顶分型), `UNKNOWN` |
| `BI_DIR` | 笔方向 | `UP`, `DOWN` |
| `BI_TYPE` | 笔类型 | `STRICT`(严格笔), `SUB_VALUE`(次高低点成笔), `TIAOKONG_THRED`(跳空笔) 等 |
| `BSP_TYPE` | 买卖点类型 | `T1`(一类), `T1P`(盘整背驰一类), `T2`(二类), `T2S`(类二类), `T3A`(三类-after), `T3B`(三类-before) |
| `AUTYPE` | 复权类型 | `QFQ`(前复权), `HFQ`(后复权), `NONE` |
| `MACD_ALGO` | 背驰判断指标 | `PEAK`(红绿柱峰值), `AREA`(面积), `SLOPE`(斜率), `AMP`(振幅) 等 |
| `DATA_FIELD` | K线数据字段名 | `FIELD_TIME`, `FIELD_OPEN`, `FIELD_CLOSE` 等 |

#### 4.1.2 CTime.py — 时间类

```python
class CTime:
    def __init__(self, year, month, day, hour, minute, second=0, auto=True):
```

**方法说明：**

| 方法 | 作用 |
|------|------|
| `set_timestamp()` | 将时间转换为时间戳。**关键逻辑**：当 `auto=True` 且时间为 0:00 时，会自动将时间改为 23:59，以解决日线（无小时信息）与分钟线的时间对齐问题 |
| `to_str()` / `toDateStr()` / `toDate()` | 日期格式化输出，方便不同场景使用 |
| `__gt__()` / `__ge__()` | 比较运算符，基于时间戳比较，用于 K 线排序 |

> **缠论知识**：`auto=True` 将日线时间自动设为 23:59，是因为日线代表的是"当天结束"，而分钟线数据的结束时间通常不是 0:00。把日线设在 23:59 可以确保当天的分钟线正确归类为该日线的子级别。

#### 4.1.3 cache.py — 缓存装饰器

```python
class make_cache:
    def __init__(self, func):
    def __get__(self, instance, cls):
    def __call__(self, *args, **kwargs):
```

**方法说明：**

| 方法 | 作用 |
|------|------|
| `__init__` | 保存被装饰函数，要求必须是 `(self)` 单参数方法 |
| `__get__` | 描述器协议，确保每个实例有独立的 `_memoize_cache` 字典 |
| `__call__` | 首次调用执行原函数并缓存结果，后续调用直接返回缓存值 |

> **使用场景**：`CBi` 和 `CKLine_Combiner` 中大量使用，缓存昂贵的派生属性（如峰值 K 线、MACD 指标等）。当底层状态变更时，调用 `clean_cache()` 清空缓存。

#### 4.1.4 func_util.py — 工具函数

| 函数 | 作用 |
|------|------|
| `kltype_lt_day(_type)` | 判断 K 线级别是否小于日线（如分钟线） |
| `kltype_lte_day(_type)` | 判断 K 线级别是否小于等于日线 |
| `check_kltype_order(type_list)` | 检查级别列表是否从大到小排列 |
| `revert_bi_dir(dir)` | 反转笔方向（UP→DOWN, DOWN→UP） |
| `has_overlap(l1, h1, l2, h2, equal)` | 判断两个区间 [l1,h1] 和 [l2,h2] 是否有重叠。`equal=True` 时边界相等也算重叠 |

> **缠论知识**：`has_overlap` 是中枢计算的核心函数。中枢的定义就是"至少三笔重叠的区间"，判断两笔是否重叠就需要这个函数。

#### 4.1.5 ChanException.py — 异常类

| 类/方法 | 作用 |
|---------|------|
| `ErrCode` | 错误码枚举，分为三段：`_CHAN_ERR_BEGIN~_CHAN_ERR_END`（缠论计算错误）、`_TRADE_ERR_BEGIN~_TRADE_ERR_END`（交易错误）、`_KL_ERR_BEGIN~_KL_ERR_END`（K线数据错误） |
| `CChanException` | 缠论异常类，包含 `errcode` 和 `msg` |
| `is_kldata_err()` | 判断是否为 K 线数据相关错误 |
| `is_chan_err()` | 判断是否为缠论计算相关错误 |

---

### 4.2 KLine/ — K线模块

#### 4.2.1 CKLine_Unit — 单根K线 (`KLine_Unit.py`)

```python
class CKLine_Unit:
    def __init__(self, kl_dict, autofix=False):
```

**属性说明：**

| 属性 | 说明 |
|------|------|
| `time` | `CTime` 类型，K 线时间 |
| `open/close/high/low` | 开/收/高/低价 |
| `trade_info` | `CTradeInfo`，包含成交量、成交额、换手率 |
| `sub_kl_list` | 次级别 K 线列表（指向子级别的 CKLine_Unit） |
| `sup_kl` | 父级别 K 线（指向父级别的 CKLine_Unit） |
| `klc` | 所属的合并 K 线（CKLine） |
| `macd/boll/rsi/kdj` | 技术指标计算结果 |
| `trend` | 趋势线指标（均值、最大值、最小值） |
| `pre/next` | 前/后一根 K 线（链表结构） |

**方法说明：**

| 方法 | 作用 |
|------|------|
| `check(autofix)` | 数据校验：确保 low 是最小值、high 是最大值。`autofix=True` 时自动修正异常数据 |
| `add_children(child)` | 添加子级别 K 线到 `sub_kl_list`，用于建立跨级别关系 |
| `set_parent(parent)` | 设置父级别 K 线 |
| `set_metric(metric_model_lst)` | **核心方法**：将 K 线数据送入所有技术指标模型计算，结果存到 `self.macd/boll/rsi/kdj` 等属性 |
| `get_parent_klc()` | 获取父 K 线的合并 K 线 |
| `include_sub_lv_time(sub_lv_t)` | 递归判断子级别是否包含某个时间 |
| `set_pre_klu(pre_klu)` | 设置前后 K 线链表关系 |

#### 4.2.2 CKLine — 合并K线 (`KLine.py`)

```python
class CKLine(CKLine_Combiner[CKLine_Unit]):
```

继承自 `CKLine_Combiner[CKLine_Unit]`，表示合并后的 K 线。

**方法说明：**

| 方法 | 作用 |
|------|------|
| `GetSubKLC()` | 获取本次级别对应的合并 K 线列表（生成器），用于跨级别遍历 |
| `get_klu_max_high()` / `get_klu_min_low()` | 获取本合并 K 线中所有原始 K 线的最高/最低价 |
| `has_gap_with_next()` | 判断与下一根合并 K 线之间是否有跳空（价格区间不重叠） |
| `check_fx_valid(item2, method, for_virtual)` | **核心方法**：检查顶底分型是否合法。支持四种检查方法（strict/loss/half/totally），详见下方 |

> **缠论知识 — 分型合法性检查**：
>
> 顶分型要求中间 K 线的高点最高，但仅此还不够。缠论对分型有更严格的要求（以顶分型为例）：
> - **strict**（默认）：底分型 3 元素的最低点中的最小值，必须低于顶分型 3 元素低点中的最小值。即"底分型一定要比顶分型低"。
> - **loss**：只比较顶底分型各自的中间 K 线
> - **half**：比较顶分型前两元素和底分型后两元素
> - **totally**：底分型的最高点必须低于顶分型的最低点（完全无重叠）
>
> **举例**：
> ```
> 顶分型: 高=[10,12,11], 低=[8,7.5,9]
> 底分型: 高=[8,9,7.5], 低=[6,7,5.5]
> strict: min(8,7.5,9)=7.5 < min(6,7,5.5)=5.5? 不对，应该是顶分型的低点高于底分型的低点...
> 实际规则：对于上升笔，底分型的最低点必须比顶分型三元素最低点的最小值还低
> ```

#### 4.2.3 CKLine_List — K线列表容器 (`KLine_List.py`)

```python
class CKLine_List:
    def __init__(self, kl_type, conf: CChanConfig):
```

这是每个级别的 K 线总容器，包含该级别所有缠论元素。

**关键属性：**

| 属性 | 说明 |
|------|------|
| `lst` | `List[CKLine]`，所有合并 K 线 |
| `bi_list` | `CBiList`，所有笔 |
| `seg_list` | `CSegListComm`，所有线段 |
| `segseg_list` | `CSegListComm`，线段的线段 |
| `zs_list` | `CZSList`，所有中枢 |
| `segzs_list` | `CZSList`，线段中枢 |
| `bs_point_lst` | `CBSPointList`，笔买卖点 |
| `seg_bs_point_lst` | `CBSPointList`，线段买卖点 |
| `metric_model_lst` | 技术指标模型列表 |

**方法说明：**

| 方法 | 作用 |
|------|------|
| `add_single_klu(klu)` | **核心方法**：将一根新 K 线加入列表。内部流程：1) 计算指标 `set_metric()` → 2) 尝试合并 K 线 → 3) 检测分形 → 4) 更新笔 → 5) 如果是步进模式，实时重算段/中枢/买卖点 |
| `cal_seg_and_zs()` | **核心方法**：最终化计算。1) 计算虚笔 → 2) 计算线段 → 3) 计算中枢 → 4) 将中枢分配到线段 → 5) 计算线段的线段与中枢 → 6) 计算买卖点 |
| `need_cal_step_by_step()` | 判断是否需要每步重算（由 `trigger_step` 配置决定） |
| `klu_iter(klc_begin_idx)` | 生成器，遍历从某个合并 K 线开始的所有原始 K 线 |

**辅助函数：**

| 函数 | 作用 |
|------|------|
| `cal_seg(bi_list, seg_list, ...)` | 计算线段，并给每笔分配 `seg_idx`（属于哪个线段） |
| `update_zs_in_seg(bi_list, seg_list, zs_list)` | 将中枢分配到其所在的线段中，并设置中枢的 `bi_in`（进入中枢的笔）和 `bi_out`（离开中枢的笔） |

#### 4.2.4 TradeInfo.py — 交易指标类

```python
class CTradeInfo:
    def __init__(self, info: Dict[str, float]):
```

存储成交量(volume)、成交额(turnover)、换手率(turnover_rate)等可选指标。这些指标如果数据源不提供，值为 `None`。

---

### 4.3 Combiner/ — 合并器模块

#### 4.3.1 Combine_Item.py — 合并元素包装

```python
class CCombine_Item:
    def __init__(self, item):
```

将 `CBi`、`CKLine_Unit`、`CSeg` 三种类型统一包装，提供 `time_begin`、`time_end`、`high`、`low` 四个属性。这使得 `CKLine_Combiner` 可以同时用于 K 线合并和特征序列合并。

#### 4.3.2 CKLine_Combiner — 包含处理合并器 (`KLine_Combiner.py`)

```python
class CKLine_Combiner(Generic[T]):
    def __init__(self, kl_unit: T, _dir):
```

这是整个项目中最重要的基类之一，实现了**缠论包含处理**逻辑。

**方法说明：**

| 方法 | 作用 |
|------|------|
| `test_combine(item, exclude_included, allow_top_equal)` | **核心方法**：判断 item 与当前合并元素的关系。返回 `COMBINE`（需要合并）、`INCLUDED`（被包含）、`UP`（向上）、`DOWN`（向下） |
| `try_add(unit_kl, ...)` | 尝试将新元素加入合并。如果 `test_combine` 返回 `COMBINE`，则合并并更新高低点；否则返回方向 |
| `update_fx(_pre, _next, ...)` | **核心方法**：检测顶底分型。判断当前元素是否满足"中间元素的高点最高+低点最高(顶分型)"或"中间元素的低点最低+高点最低(底分型)" |
| `get_high_peak_klu()` / `get_low_peak_klu()` | 获取合并 K 线中实际最高/最低点所在的原始 K 线（用于确定笔的精确起止位置） |
| `clean_cache()` | 清空 memoize 缓存 |
| `set_pre()` / `set_next()` | 设置前后链表关系 |

> **缠论知识 — K线包含处理详解**：
>
> 缠论中，K 线包含处理是第一步。当相邻两根 K 线出现包含关系（一根的高低点完全覆盖另一根），需要合并：
>
> 1. 确定当前趋势方向：看前一根非包含的 K 线方向
> 2. 上升趋势：取高高（高点=max，低点=max）
> 3. 下降趋势：取低低（高点=min，低点=min）
>
> **举例**：
> ```
> 前一根方向向上，当前K线包含下一根K线：
> 当前K线: 高=10, 低=7
> 下一根: 高=9, 低=8
> → 合并：高=max(10,9)=10, 低=max(7,8)=8
> ```

---

### 4.4 Bi/ — 笔模块

#### 4.4.1 BiConfig.py — 笔配置

```python
class CBiConfig:
    def __init__(self, bi_algo, is_strict, bi_fx_check, gap_as_kl, bi_end_is_peak, bi_allow_sub_peak):
```

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `bi_algo` | `"normal"` | 笔算法：normal(按缠论定义)、fx(顶底分形成即笔) |
| `is_strict` | `True` | 严格笔：顶底分型间至少隔 4 根合并 K 线 |
| `bi_fx_check` | `"strict"` | 分型检查方法：strict/loss/half/totally |
| `gap_as_kl` | `True` | 缺口是否算一根 K 线（增加 K 线间距，使跨度满足条件） |
| `bi_end_is_peak` | `True` | 笔的尾部是否是整笔中最低/最高点 |
| `bi_allow_sub_peak` | `True` | 是否允许次高点成笔 |

#### 4.4.2 Bi.py — 笔类

```python
class CBi:
    def __init__(self, begin_klc, end_klc, idx, is_sure):
```

**属性说明：**

| 属性 | 说明 |
|------|------|
| `begin_klc` / `end_klc` | 起止合并 K 线 |
| `dir` | 方向：`BI_DIR.UP` 或 `BI_DIR.DOWN` |
| `is_sure` | 是否已确定（确定的笔不会被后续 K 线改变） |
| `seg_idx` | 所属线段编号 |
| `parent_seg` | 指向所属线段对象 |
| `bsp` | 尾部是否是买卖点 |

**方法说明：**

| 方法 | 作用 |
|------|------|
| `set(begin_klc, end_klc)` | 设置笔的起止 K 线，自动判断方向（底分型开头→上升笔，顶分型开头→下降笔） |
| `get_begin_val()` / `get_end_val()` | 获取笔的起始/结束价格。上升笔：起点=底分型低点，终点=顶分型高点。下降笔反之 |
| `get_begin_klu()` / `get_end_klu()` | 获取笔的起始/结束位置的原始 K 线（精确到峰值点） |
| `_high()` / `_low()` | 笔的最高/最低价 |
| `_mid()` | 笔的中位价（(high+low)/2），用于中枢计算 |
| `amp()` | 笔的振幅（绝对涨跌幅） |
| `update_virtual_end(new_klc)` | 更新虚笔的终点（新 K 线创了新高/新低） |
| `restore_from_virtual_end(sure_end)` | 从虚笔恢复为确定笔 |
| `cal_macd_metric(macd_algo, is_reverse)` | **核心方法**：计算笔的 MACD 指标，用于背驰判断。支持 peak/area/full_area/diff/slope/amp 等算法 |
| `Cal_MACD_peak()` | 计算笔范围内 MACD 红绿柱的峰值（绝对值） |
| `Cal_MACD_area()` | 计算笔范围内 MACD 红绿柱的面积（同向柱子绝对值之和） |
| `Cal_MACD_half_obverse()` | 计算笔前半段（从起点到 MACD 首次归零）的面积 |
| `Cal_MACD_half_reverse()` | 计算笔后半段（从终点反向到 MACD 首次归零）的面积 |
| `Cal_MACD_slope()` | 计算笔的斜率 = (终点价-起点价)/(起点价*K线数) |
| `Cal_MACD_amp()` | 计算笔的涨跌幅 |

> **缠论知识 — 背驰判断**：
>
> 背驰意味着趋势力度的减弱。以一类买点为例，判断方法是：
> 1. 找到进入中枢的那笔（bi_in）和离开中枢的那笔（bi_out）
> 2. 分别计算两笔的 MACD 指标（如 peak 峰值）
> 3. 如果 out_metric < divergence_rate * in_metric（默认 0.9），则判定为背驰
>
> 例如：进入中枢的笔 MACD 峰值为 100，离开中枢的笔 MACD 峰值为 80，背驰率=0.8 < 0.9，判定为背驰 → 一类买点。

#### 4.4.3 BiList.py — 笔列表管理类

```python
class CBiList:
    def __init__(self, bi_conf=CBiConfig()):
```

**方法说明：**

| 方法 | 作用 |
|------|------|
| `update_bi(klc, last_klc, cal_virtual)` | **核心方法**：每次新增合并 K 线时调用。1) 更新确定笔 → 2) 如果是步进模式，更新虚笔 |
| `update_bi_sure(klc)` | 更新确定笔：根据新 K 线的分型类型，决定是创建新笔、更新笔尾、还是更新峰值 |
| `try_create_first_bi(klc)` | 尝试创建第一笔（第一笔需要等至少两个不同分型出现） |
| `try_add_virtual_bi(klc, need_del_end)` | 尝试添加虚笔（未确定的笔）。当新 K 线创新高/新低时，更新最后一笔的终点 |
| `delete_virtual_bi()` | 删除虚笔，恢复为之前的确定状态 |
| `can_make_bi(klc, last_end, for_virtual)` | 判断是否满足成笔条件：1) 跨度检查 → 2) 分型合法性检查 → 3) 尾部是极值检查 |
| `satisfy_bi_span(klc, last_end)` | 检查笔的跨度：严格笔要求至少 4 根合并 K 线，非严格笔要求至少 3 根合并 K 线+3 根原始 K 线 |
| `get_klc_span(klc, last_end)` | 计算两根 K 线之间的跨度，如果 `gap_as_kl=True`，每个跳空额外算 1 |
| `try_update_end(klc, for_virtual)` | 尝试更新笔的终点（同向分型出现且价格更极端时） |
| `can_update_peak(klc)` | 判断是否可以用次高点/低点更新笔的终点 |
| `update_peak(klc, for_virtual)` | 用次高点/低点更新笔的终点 |
| `add_new_bi(pre_klc, cur_klc, is_sure)` | 创建新笔并加入列表 |

**辅助函数：**

| 函数 | 作用 |
|------|------|
| `end_is_peak(last_end, cur_end)` | 检查笔的终点是否是整笔中的极值（最低/最高），确保中间没有更高的高点和更低的低点 |

> **缠论知识 — 笔的确定与虚笔**：
>
> 笔的确定有滞后性。例如当前是一笔上升笔，如果后续 K 线继续创新高，这笔的终点会不断上移。此时这笔是"虚笔"（`is_sure=False`）。只有当出现反向分型且满足成笔条件后，这笔才确定为"实笔"（`is_sure=True`）。
>
> 在画图中，虚笔用虚线表示，实笔用实线表示。

---

### 4.5 Seg/ — 线段模块

#### 4.5.1 Seg.py — 线段类

```python
class CSeg(Generic[LINE_TYPE]):
    def __init__(self, idx, start_bi, end_bi, is_sure, seg_dir, reason):
```

**属性说明：**

| 属性 | 说明 |
|------|------|
| `start_bi` / `end_bi` | 起始笔/终止笔 |
| `dir` | 方向 |
| `is_sure` | 是否确定 |
| `zs_lst` | 线段内的中枢列表 |
| `eigen_fx` | 特征序列分型（用于线段的段的计算） |
| `bi_list` | 线段内的笔列表 |
| `support_trend_line` | 支撑趋势线 |
| `resistance_trend_line` | 阻力趋势线 |

**方法说明：**

| 方法 | 作用 |
|------|------|
| `check()` | 校验线段合法性：长度至少 3 笔，上升段起点低终点高，下降段反之 |
| `add_zs(zs)` | 添加中枢到线段 |
| `_high()` / `_low()` | 线段的最高/最低价（上升段取终点，下降段取起点） |
| `get_begin_val()` / `get_end_val()` | 线段的起始/结束价格 |
| `update_bi_list(bi_lst, idx1, idx2)` | 更新线段内的笔列表，并计算趋势线 |
| `get_first_multi_bi_zs()` | 获取第一个多笔中枢（排除一笔中枢） |
| `get_final_multi_bi_zs()` | 获取最后一个多笔中枢 |
| `get_multi_bi_zs_cnt()` | 获取多笔中枢数量 |
| `cal_macd_metric(macd_algo, is_reverse)` | 计算线段的 MACD 指标（仅支持 slope/amp） |

#### 4.5.2 SegListComm.py — 线段列表通用父类

```python
class CSegListComm(Generic[SUB_LINE_TYPE]):
    def __init__(self, seg_config=CSegConfig(), lv=SEG_TYPE.BI):
```

**方法说明：**

| 方法 | 作用 |
|------|------|
| `update(bi_lst)` | **抽象方法**，子类实现。根据笔列表更新线段列表 |
| `try_add_new_seg(bi_lst, end_bi_idx, ...)` | 尝试添加新线段。处理首段拆分逻辑 |
| `add_new_seg(bi_lst, end_bi_idx, ...)` | 添加新线段到列表 |
| `collect_first_seg(bi_lst)` | 收集第一段（开头不确定的虚段） |
| `collect_left_seg(bi_lst)` | 收集剩余未归入确定线段的笔为虚段 |
| `collect_segs(bi_lst)` | 处理尾部虚段，根据不同的 `left_method` 策略 |
| `collect_left_seg_peak_method(...)` | PEAK 方法：找新的极值点形成新虚段 |
| `collect_left_as_seg(bi_lst)` | ALL 方法：将剩余笔直接归入一段 |
| `left_bi_break(bi_lst)` | 判断最后一个确定线段后的笔是否突破了该线段 |
| `exist_sure_seg()` | 是否存在确定线段 |

**辅助函数：**

| 函数 | 作用 |
|------|------|
| `FindPeakBi(bi_lst, is_high)` | 在笔列表中找最高/最低的笔（用于确定虚段终点） |

> **缠论知识 — 虚段处理**：
>
> 线段也存在"虚段"（未确定段）。当已有笔不足以确定一个新线段时，需要根据剩余笔推断可能的线段。有两种策略：
> - **peak**（默认）：找一个新的极值点，如果靠谱就形成一段
> - **all**：收集所有剩余笔为一段
>
> 作者在注释中特别提到，`CSegListComm` 是"整个项目里面逻辑最复杂最难的部分"。

#### 4.5.3 SegListChan.py — 特征序列线段算法

```python
class CSegListChan(CSegListComm):
    def __init__(self, seg_config=CSegConfig(), lv=SEG_TYPE.BI):
```

这是默认的线段算法，基于缠论原文的特征序列方法。

**方法说明：**

| 方法 | 作用 |
|------|------|
| `update(bi_lst)` | **核心方法**：1) 重置不确定段 → 2) 从上次确定段之后重新计算确定段 → 3) 收集剩余虚段 |
| `do_init()` | 删除末尾不确定的线段，为重新计算做准备 |
| `cal_seg_sure(bi_lst, begin_idx)` | **核心方法**：从指定笔开始，用特征序列法找确定线段。创建两个 `CEigenFX`（上升和下降），遍历笔，寻找特征序列分型 |
| `treat_fx_eigen(fx_eigen, bi_lst)` | 处理找到的特征序列分型：判断是否可以作为线段终点，创建新线段 |

> **缠论知识 — 特征序列线段算法**：
>
> 这是缠论中最核心也最复杂的算法之一。以上升线段为例：
> 1. 上升线段由上升笔组成，中间的下降笔构成"特征序列"
> 2. 对特征序列进行包含处理（类似 K 线合并）
> 3. 在特征序列中找顶分型
> 4. 如果顶分型第二元素之间有**跳空**（gap），需要等待反向底分型确认
> 5. 如果没有跳空，顶分型出现即确认
>
> 代码中 `CEigenFX` 管理特征序列分型，`CEigen` 表示单个特征序列元素。

#### 4.5.4 Eigen.py — 特征序列元素

```python
class CEigen(CKLine_Combiner[CBi]):
```

继承自 `CKLine_Combiner[CBi]`，将笔当 K 线进行包含处理。

**方法说明：**

| 方法 | 作用 |
|------|------|
| `update_fx(_pre, _next, ...)` | 检测特征序列分型，同时判断是否有跳空（gap） |
| `GetPeakBiIdx()` | 获取分型中峰值笔的索引（用于确定线段终点） |

#### 4.5.5 EigenFX.py — 特征序列分型

```python
class CEigenFX:
    def __init__(self, _dir, exclude_included=True, lv=SEG_TYPE.BI):
```

管理特征序列的三元素（类似 K 线分型的三元素）。

**方法说明：**

| 方法 | 作用 |
|------|------|
| `add(bi)` | **核心方法**：向特征序列添加一笔。根据当前所处的阶段（第一/二/三元素），调用对应的处理方法 |
| `treat_first_ele(bi)` | 处理第一元素：创建新的 CEigen |
| `treat_second_ele(bi)` | 处理第二元素：尝试与第一元素合并，不能合并则创建第二元素 |
| `treat_third_ele(bi)` | 处理第三元素：尝试与第二元素合并，形成分型后检查是否实际突破 |
| `reset()` | 分型不成立时重置，将第二元素变为第一元素，继续寻找 |
| `can_be_end(bi_lst)` | 判断特征序列分型是否可以作为线段终点：如果 gap 则需找反向分型确认，否则直接确认 |
| `find_revert_fx(bi_lst, begin_idx, ...)` | 跳空时寻找反向分型确认 |
| `actual_break()` | 检查第三元素是否实际突破了第二元素的低点/高点 |
| `all_bi_is_sure()` | 检查特征序列中所有笔是否都已确定 |
| `GetPeakBiIdx()` | 获取分型中峰值笔的索引 |

> **缠论知识 — 特征序列分型详解**：
>
> 以**上升线段**为例（线段方向向上，特征序列由下降笔组成）：
> - 第一元素：第一根下降笔
> - 第二元素：第二根下降笔（可能与第一元素合并）
> - 第三元素：第三根下降笔
> - 如果第二元素形成顶分型（高点最高），则上升线段可能结束
>
> **跳空与确认**：
> - 如果第二元素与第一元素之间没有重叠（跳空），需要等待反向底分型确认
> - 如果没有跳空，顶分型出现即确认线段结束
>
> ```
> 上升线段特征序列（下降笔）：
> 笔1: 高10 → 低7
> 笔2: 高9 → 低5  (特征序列元素1: 高9, 低5)
> 笔3: 高8 → 低6  (特征序列元素2: 高8, 低6)  ← 包含处理
> 笔4: 高7 → 低4  (特征序列元素3: 高7, 低4)
> 如果高8 < 高9 → 顶分型不成立，继续
> ```

#### 4.5.6 SegConfig.py — 线段配置

```python
class CSegConfig:
    def __init__(self, seg_algo="chan", left_method="peak"):
```

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `seg_algo` | `"chan"` | 线段算法：chan(特征序列)、1+1(都业华)、break(线段破坏) |
| `left_method` | `"peak"` | 尾段处理方法：all/peak |

---

### 4.6 ZS/ — 中枢模块

#### 4.6.1 ZS.py — 中枢类

```python
class CZS(Generic[LINE_TYPE]):
    def __init__(self, lst, is_sure=True):
```

**属性说明：**

| 属性 | 说明 |
|------|------|
| `begin` / `end` | 中枢的起始/结束 K 线 |
| `begin_bi` / `end_bi` | 中枢内部的第一笔/最后一笔 |
| `low` / `high` | 中枢区间：[low, high] = [max(各笔低点), min(各笔高点)] |
| `mid` | 中枢中点 (low+high)/2 |
| `peak_low` / `peak_high` | 中枢内所有笔的最低/最高值 |
| `bi_in` | 进入中枢的笔（中枢外部的前一笔） |
| `bi_out` | 离开中枢的笔（中枢外部的后一笔） |
| `bi_lst` | 中枢内的笔列表 |
| `sub_zs_lst` | 子中枢列表（中枢合并时产生） |

**方法说明：**

| 方法 | 作用 |
|------|------|
| `update_zs_range(lst)` | 更新中枢区间：low = max(各笔低点), high = min(各笔高点) |
| `update_zs_end(item)` | 更新中枢的结束位置和极值 |
| `is_one_bi_zs()` | 是否是一笔中枢（begin_bi == end_bi） |
| `combine(zs2, combine_mode)` | 尝试与另一个中枢合并。`zs` 模式要求区间重叠，`peak` 模式要求 K 线重叠 |
| `do_combine(zs2)` | 执行合并：扩展区间范围，保存子中枢 |
| `try_add_to_end(item)` | 尝试将一笔延伸到中枢 |
| `in_range(item)` | 判断一笔是否在中枢范围内 |
| `is_inside(seg)` | 判断中枢是否属于某个线段 |
| `is_divergence(config, out_bi)` | **核心方法**：判断是否背驰。比较 bi_in 和 bi_out 的 MACD 指标 |
| `end_bi_break(end_bi)` | 判断离开中枢的笔是否突破了中枢 |
| `out_bi_is_peak(end_bi_idx)` | 判断离开中枢的笔是否是中枢内的极值点 |
| `init_from_zs(zs)` | 从另一个中枢复制属性（用于合并） |
| `make_copy()` | 创建中枢的副本 |
| `set_bi_in(bi)` / `set_bi_out(bi)` / `set_bi_lst(bi_lst)` | 设置中枢的进入笔/离开笔/笔列表 |

> **缠论知识 — 中枢的形成与合并**：
>
> 中枢是缠论中最重要的概念，是走势的"引力中心"。
>
> **形成条件**：连续三笔重叠（下上下或上下上），且 `min(高) > max(低)`。
>
> **中枢合并**：当两个相邻中枢的区间有重叠时，可以合并为一个更大的中枢。合并规则有两种：
> - **zs**（默认）：两中枢区间 [low, high] 有重叠才合并
> - **peak**：两中枢的 K 线范围有重叠就合并
>
> **举例**：
> ```
> 中枢1: [8, 10]  区间: 8~10
> 中枢2: [9, 11]  区间: 9~11
> 重叠区间: [9, 10] → 合并后: [8, 11]
> ```

#### 4.6.2 ZSList.py — 中枢列表管理类

```python
class CZSList:
    def __init__(self, zs_config=CZSConfig()):
```

**方法说明：**

| 方法 | 作用 |
|------|------|
| `cal_bi_zs(bi_lst, seg_lst)` | **核心方法**：根据线段列表计算中枢。支持三种算法：normal(段内)、over_seg(跨段)、auto(自动) |
| `update(bi, is_sure)` | 更新中枢（新增一笔时调用） |
| `add_to_free_lst(item, is_sure, zs_algo)` | 将一笔加入待处理列表，尝试构造中枢 |
| `try_construct_zs(lst, is_sure, zs_algo)` | 尝试从笔列表构造中枢。normal 模式取最后 2 笔，over_seg 模式取最后 3 笔 |
| `try_add_to_end(bi)` | 尝试将笔延伸到最后一个中枢（peak 合并模式） |
| `try_combine()` | 尝试合并相邻中枢 |
| `add_zs_from_bi_range(seg_bi_lst, seg_dir, seg_is_sure)` | 从一个线段的笔范围中计算中枢 |
| `update_overseg_zs(bi)` | 跨段中枢算法的更新逻辑 |
| `update_last_pos(seg_list)` | 更新最后一个确定位置，避免重复计算 |

> **缠论知识 — 段内中枢 vs 跨段中枢**：
>
> - **段内中枢（normal）**：中枢不跨段，每个线段内部独立计算中枢。这是原文标准做法。
> - **跨段中枢（over_seg）**：允许中枢跨越线段边界。当新中枢产生时，起始笔考虑线段方向。
> - **auto**：确定线段用 normal，不确定部分用 over_seg。

#### 4.6.3 ZSConfig.py — 中枢配置

```python
class CZSConfig:
    def __init__(self, need_combine=True, zs_combine_mode="zs", one_bi_zs=False, zs_algo="normal"):
```

---

### 4.7 BuySellPoint/ — 买卖点模块

#### 4.7.1 BS_Point.py — 买卖点类

```python
class CBS_Point(Generic[LINE_TYPE]):
    def __init__(self, bi, is_buy, bs_type, relate_bsp1, feature_dict):
```

**属性说明：**

| 属性 | 说明 |
|------|------|
| `bi` | 所属的笔 |
| `klu` | 所在 K 线 |
| `is_buy` | True=买点，False=卖点 |
| `type` | 买卖点类型列表（一个位置可能同时是多种类型，如 2+3 类买卖点重合） |
| `relate_bsp1` | 关联的一类买卖点 |
| `features` | 特征字典，用于 ML 模型 |

**方法说明：**

| 方法 | 作用 |
|------|------|
| `add_type(bs_type)` | 添加买卖点类型（同一位置多种类型） |
| `type2str()` | 类型转字符串，如 "1,2" |
| `add_another_bsp_prop(bs_type, relate_bsp1)` | 添加另一种买卖点属性 |
| `add_feat(inp1, inp2)` | 添加特征 |
| `init_common_feature()` | 初始化通用特征（如 bsp_bi_amp） |

#### 4.7.2 BSPointList.py — 买卖点列表计算类

```python
class CBSPointList(Generic[LINE_TYPE, LINE_LIST_TYPE]):
    def __init__(self, bs_point_config: CBSPointConfig):
```

**方法说明：**

| 方法 | 作用 |
|------|------|
| `cal(bi_list, seg_list)` | **核心入口**：计算所有买卖点。依次调用 cal_seg_bs1point → cal_seg_bs2point → cal_seg_bs3point |
| `cal_seg_bs1point(seg_list, bi_list)` | 遍历所有需要计算的线段，计算一类买卖点 |
| `cal_single_bs1point(seg, bi_list)` | **核心方法**：计算单个线段的一类买卖点。判断逻辑：1) 中枢数是否满足 → 2) 离开中枢的笔是否突破中枢 → 3) 是否背驰 → 4) 是否极值点 |
| `treat_bsp1(seg, BSP_CONF, is_target_bsp)` | 处理有中枢的线段的一类买卖点（标准背驰一类） |
| `treat_pz_bsp1(seg, BSP_CONF, bi_list, is_target_bsp)` | 处理盘整背驰的一类买卖点（T1P，中枢内背驰） |
| `cal_seg_bs2point(seg_list, bi_list)` | 计算二类买卖点：一类买卖点之后的第一笔回撤 |
| `treat_bsp2(seg, seg_list, bi_list)` | 处理二类买卖点：回撤比例不超过 `max_bs2_rate` |
| `treat_bsp2s(...)` | 处理类二买卖点：中枢内同侧的其他买卖点 |
| `cal_seg_bs3point(seg_list, bi_list)` | 计算三类买卖点 |
| `treat_bsp3_after(...)` | 处理 T3A（中枢在一类之后）：离开中枢后回抽不进入中枢 |
| `treat_bsp3_before(...)` | 处理 T3B（中枢在一类之前）：离开中枢后回抽不进入中枢 |
| `add_bs(bs_type, bi, relate_bsp1, ...)` | 添加买卖点到存储中 |
| `getSortedBspList()` | 获取按时间排序的买卖点列表 |
| `get_latest_bsp(number)` | 获取最近的 N 个买卖点 |
| `clear_store_end()` / `clear_bsp1_end()` | 清理尾部不确定的买卖点 |
| `update_last_pos(seg_list)` | 更新最后一个确定位置 |

**辅助函数：**

| 函数 | 作用 |
|------|------|
| `bsp2s_break_bsp1(bsp2s_bi, bsp2_break_bi)` | 判断类二买卖点是否突破了一类买卖点 |
| `bsp3_back2zs(bsp3_bi, zs)` | 判断三类买卖点是否回到了中枢区间内 |
| `bsp3_break_zspeak(bsp3_bi, zs)` | 判断三类买卖点是否突破了中枢极值 |
| `cal_bsp3_bi_end_idx(seg)` | 计算三类买卖点搜索的结束位置 |

> **缠论知识 — 买卖点类型详解**：
>
> **一类买卖点（T1）**：趋势的转折点
> - 条件：1) 离开中枢的笔突破了中枢 2) 离开中枢的笔相对于进入中枢的笔发生背驰（MACD 指标减弱） 3) 足够的中枢数量
> - 一买：下跌趋势结束，开始上涨
> - 一卖：上涨趋势结束，开始下跌
>
> **一类买卖点-盘整背驰（T1P）**：
> - 没有完整中枢，但相邻两笔同向且背驰
> - 例如：两笔相邻下降笔，第二笔创新低但 MACD 弱于第一笔
>
> **二类买卖点（T2）**：
> - 一类买卖点之后的第一笔回撤
> - 回撤幅度不超过 `max_bs2_rate`（默认 0.618，即黄金分割位）
>
> **类二买卖点（T2S）**：
> - 二类买卖点之后，中枢内同侧的其他买卖点
> - 与二类买卖点在同一个中枢内重叠
>
> **三类买卖点（T3A/T3B）**：
> - T3A：中枢在一类买卖点**之后**，离开中枢后回抽不进入中枢
> - T3B：中枢在一类买卖点**之前**，离开中枢后回抽不进入中枢
>
> **举例（一买）**：
> ```
> 走势：下跌线段 → 中枢 → 继续下跌（背驰）→ 一买
> 线1: 从高到低进入中枢 (进入笔)
> 中枢: [8, 10] (中间三笔震荡)
> 线2: 从高到低离开中枢 (离开笔)，创新低但MACD减弱 → 背驰
> 线2终点 = 一类买点
> ```

#### 4.7.3 BSPointConfig.py — 买卖点配置

```python
class CBSPointConfig:
    def __init__(self, **args):
        self.b_conf = CPointConfig(**args)  # 买点配置
        self.s_conf = CPointConfig(**args)  # 卖点配置

class CPointConfig:
    def __init__(self, divergence_rate, min_zs_cnt, bsp1_only_multibi_zs,
                 max_bs2_rate, macd_algo, bs1_peak, bs_type, ...):
```

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `divergence_rate` | 0.9 | 背驰率阈值，out_metric/in_metric < 0.9 判定为背驰 |
| `min_zs_cnt` | 1 | 一类买卖点至少需要经历的中枢数量 |
| `max_bs2_rate` | 0.9999 | 二类买卖点最大回撤比例 |
| `macd_algo` | `"peak"` | 背驰计算指标算法 |
| `bs1_peak` | True | 一类买卖点是否必须是中枢内极值 |
| `bs_type` | `"1,1p,2,2s,3a,3b"` | 需要计算的买卖点类型 |
| `bsp2_follow_1` | True | 二类买卖点是否必须跟在一类买卖点后面 |
| `bsp3_follow_1` | True | 三类买卖点是否必须跟在一类买卖点后面 |
| `bsp3_peak` | False | 三类买卖点突破笔是否必须突破中枢极值 |

---

### 4.8 Math/ — 技术指标模块

#### 4.8.1 MACD.py — MACD 指标

```python
class CMACD:
    def __init__(self, fastperiod=12, slowperiod=26, signalperiod=9):
    def add(self, value) -> CMACD_item:
```

**算法说明：**
- EMA（指数移动平均）：`EMA_today = (2*price + (period-1)*EMA_yesterday) / (period+1)`
- DIF = 快线 EMA(12) - 慢线 EMA(26)
- DEA = DIF 的 EMA(9)
- MACD 柱 = 2 * (DIF - DEA)

> **缠论知识**：MACD 是缠论中最常用的背驰判断指标。项目默认使用 MACD 红绿柱的峰值（peak）来比较两笔的力度。

#### 4.8.2 BOLL.py — 布林线

```python
class BollModel:
    def __init__(self, N=20):
    def add(self, value) -> BOLL_Metric:
```

计算 N 日均值和标准差，形成上轨(MID+2σ)、中轨、下轨(MID-2σ)。

#### 4.8.3 RSI.py — 相对强弱指标

```python
class RSI:
    def __init__(self, period=14):
    def add(self, close):
```

通过比较一段时期内的平均上涨幅度和平均下跌幅度来计算 RSI 值。

#### 4.8.4 KDJ.py — 随机指标

```python
class KDJ:
    def __init__(self, period=9):
    def add(self, high, low, close) -> KDJ_Item:
```

基于 N 日最高价、最低价和收盘价，计算 RSV → K → D → J。

#### 4.8.5 TrendModel.py — 趋势模型

```python
class CTrendModel:
    def __init__(self, trend_type: TREND_TYPE, T: int):
    def add(self, value) -> float:
```

计算 T 周期内的均值(MEAN)、最大值(MAX)、最小值(MIN)。支持配置多个周期，如 `mean_metrics=[5,20]` 表示同时计算 5 日和 20 日均线。

#### 4.8.6 Demark.py — 德马克指标

```python
class CDemarkEngine:
    def __init__(self, demark_len=9, setup_bias=4, countdown_bias=2, max_countdown=13, ...):
    def update(self, idx, close, high, low) -> CDemarkIndex:
```

德马克指标分为 Setup 阶段（9 天）和 Countdown 阶段（13 天），用于识别趋势衰竭。

#### 4.8.7 TrendLine.py — 趋势线

```python
class CTrendLine:
    def __init__(self, lst, side=TREND_LINE_SIDE.OUTSIDE):
    def cal(self, lst):
```

计算线段的支撑线和阻力线。`INSIDE` 表示内部趋势线（连接笔的起点），`OUTSIDE` 表示外部趋势线（连接笔的终点）。

---

### 4.9 DataAPI/ — 数据源模块

#### 4.9.1 CommonStockAPI.py — 抽象数据源基类

```python
class CCommonStockApi:
    def __init__(self, code, k_type, begin_date, end_date, autype):
```

**方法说明：**

| 方法 | 作用 |
|------|------|
| `get_kl_data()` | **抽象方法**：返回 K 线数据的生成器，yield `CKLine_Unit` |
| `SetBasciInfo()` | **抽象方法**：设置股票名称等基本信息 |
| `do_init()` | **类方法**：数据源初始化（如登录） |
| `do_close()` | **类方法**：数据源关闭（如登出） |

#### 4.9.2 BaoStockAPI.py — 默认数据源

```python
class CBaoStock(CCommonStockApi):
```

| 方法 | 作用 |
|------|------|
| `get_kl_data()` | 从 BaoStock 获取 K 线数据，按级别选择字段（日线以下无成交量信息） |
| `SetBasciInfo()` | 查询股票基本信息（名称、类型） |
| `do_init()` / `do_close()` | 登录/登出 BaoStock |
| `__convert_type()` | 将 KL_TYPE 转为 BaoStock 的 frequency 参数 |

#### 4.9.3 自定义数据源

在 `CChan.GetStockAPI()` 中，支持 `"custom:文件名.类名"` 格式动态导入自定义数据源。自定义类只需继承 `CCommonStockApi` 并实现 `get_kl_data()` 和 `SetBasciInfo()` 即可。

---

### 4.10 Plot/ — 绘图模块

#### 4.10.1 PlotDriver.py — 静态绘图

```python
class CPlotDriver:
    def __init__(self, chan, plot_config, plot_para):
```

基于 matplotlib，将所有缠论元素绘制到一张图上。主要方法：

| 方法 | 作用 |
|------|------|
| `DrawElement(plot_config, meta, ax, lv, plot_para, ...)` | 根据配置逐一绘制：K线、合并K线、笔、线段、中枢、买卖点、MACD、布林线、均线等 |
| `save2img(path)` | 保存为图片文件 |
| `Upload2COS(path)` | 上传图片到 COS 云存储 |

#### 4.10.2 AnimatePlotDriver.py — 动画绘图

```python
class CAnimateDriver:
    def __init__(self, chan, plot_config, plot_para):
```

逐帧播放 K 线增加过程，用于 Jupyter Notebook 中的动态演示。使用 `IPython.display.clear_output` 实现。

#### 4.10.3 PlotMeta.py — 绘图元数据

为每个缠论元素提供绘图所需的坐标信息，将 K 线时间映射到 X 轴坐标。包含 `Cklc_meta`、`CBi_meta`、`CSeg_meta`、`CZS_meta`、`CEigen_meta`、`CEigenFX_meta` 等类。

---

### 4.11 Chan.py — 主入口类

```python
class CChan:
    def __init__(self, code, begin_time, end_time, data_src, lv_list, config, autype):
```

**参数说明：**

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `code` | - | 股票代码，格式取决于数据源 |
| `begin_time` | None | 开始时间 |
| `end_time` | None | 结束时间 |
| `data_src` | `DATA_SRC.BAO_STOCK` | 数据源 |
| `lv_list` | `[K_DAY, K_60M]` | K 线级别，必须从大到小 |
| `config` | `CChanConfig()` | 缠论配置 |
| `autype` | `AUTYPE.QFQ` | 复权类型 |

**方法说明：**

| 方法 | 作用 |
|------|------|
| `load(step)` | **核心方法**：加载数据并计算。1) 获取数据源 API → 2) 初始化各级别 K 线迭代器 → 3) 调用 `load_iterator` 递归加载 → 4) 批量模式下最终计算段和中枢 |
| `load_iterator(lv_idx, parent_klu, step)` | **核心递归方法**：从最高级别向最低级别递归加载 K 线。每来一根 K 线：1) 设置 pre/next 关系 → 2) 调用 `add_new_kl` 加入 K 线列表 → 3) 建立父子关系 → 4) 递归处理子级别 → 5) 步进模式下 yield 当前快照 |
| `step_load()` | 步进模式加载（生成器），每根 K 线返回一个 CChan 快照 |
| `trigger_load(inp)` | 外部喂 K 线触发计算（实盘使用） |
| `GetStockAPI()` | 根据 data_src 参数解析并返回对应的数据源类 |
| `init_lv_klu_iter(stockapi_cls)` | 初始化各级别 K 线迭代器，跳过获取失败的级别 |
| `set_klu_parent_relation(parent_klu, kline_unit, cur_lv, lv_idx)` | 建立父子 K 线关系，并检查时间一致性 |
| `add_new_kl(cur_lv, kline_unit)` | 将新 K 线加入对应级别的 K 线列表 |
| `try_set_klu_idx(lv_idx, kline_unit)` | 为 K 线设置索引 |
| `check_kl_consitent(parent_klu, sub_klu)` | 检查父子级别 K 线日期是否一致 |
| `check_kl_align(kline_unit, lv_idx)` | 检查父 K 线是否有对应的子级别 K 线 |
| `__getitem__(n)` | 支持 `chan[KL_TYPE]` 或 `chan[0]` 获取级别数据 |
| `get_latest_bsp(idx, number)` | 获取最近的买卖点 |
| `__deepcopy__(memo)` | 自定义深拷贝，手动重建跨级别指针关系 |
| `chan_dump_pickle(file_path)` / `chan_load_pickle(file_path)` | 序列化/反序列化，临时移除链表指针避免递归溢出 |
| `chan_pickle_restore()` | 恢复序列化后的链表指针 |

> **递归加载逻辑详解**：
>
> `load_iterator` 是整个项目的核心递归函数。它从最高级别（如日线）开始：
> 1. 读取一根日线 K 线
> 2. 递归调用自身，加载这根日线时间范围内的所有子级别 K 线（如 60 分钟线）
> 3. 子级别也可能再递归加载更小的级别（如 5 分钟线）
> 4. 每根 K 线都建立 `sup_kl`（指向父 K 线）和 `sub_kl_list`（子 K 线列表）
> 5. 这就形成了跨级别的树形结构，支持区间套策略

### 4.12 ChanConfig.py — 配置系统

```python
class CChanConfig:
    def __init__(self, conf=None):
```

**方法说明：**

| 方法 | 作用 |
|------|------|
| `GetMetricModel()` | 创建所有技术指标模型实例列表（MACD + 均线 + 布林线 + Demark + RSI + KDJ） |
| `set_bsp_config(conf)` | 解析买卖点相关配置，支持 `-buy`/`-sell`/`-seg` 后缀的精确配置覆盖 |

```python
class ConfigWithCheck:
    def __init__(self, conf):
    def get(self, k, default_value):  # 获取配置并删除已消费的 key
    def check(self):  # 检查是否有未消费的 key（防止拼写错误）
```

`ConfigWithCheck` 是一个巧妙的配置校验器：每次 `get` 都会删除已消费的 key，最后 `check` 时如果还有剩余 key，说明用户输入了无效的配置项，抛出异常。

---

### 4.13 ChanModel/ — 模型特征模块

#### 4.13.1 Features.py — 特征容器

```python
class CFeatures:
    def __init__(self, initFeat=None):
    def add_feat(self, inp1, inp2=None):
    def items(self):
```

一个简单的特征字典包装器，用于存储买卖点的特征数据。完整版中会计算 500+ 个特征。

---

## 第五部分：完整计算流水线详解

### 5.1 批量模式完整流程

以 `main.py` 为例：

```
1. 创建 CChanConfig
   └─ 配置笔/段/中枢/买卖点参数

2. 创建 CChan(code, data_src, lv_list, config)
   └─ __init__ → do_init() → load()
      │
      ├─ GetStockAPI() → 确定数据源类
      ├─ 数据源 do_init() (如 baostock.login)
      ├─ init_lv_klu_iter() → 为每个级别创建 K 线迭代器
      │
      ├─ load_iterator(lv_idx=0, parent=None, step=False)
      │   │
      │   ├─ [日线] 读取一根日线 K 线
      │   │   ├─ CKLine_Unit.set_metric() → 计算各类指标
      │   │   ├─ add_new_kl() → CKLine_List.add_single_klu()
      │   │   │   ├─ CKLine_Combiner.try_add() → K 线合并
      │   │   │   ├─ CKLine_Combiner.update_fx() → 分形检测
      │   │   │   └─ CBiList.update_bi() → 笔更新
      │   │   │
      │   │   └─ load_iterator(lv_idx=1, parent=日线, step=False)
      │   │       └─ [60M] 读取 60M K 线（在日线时间范围内）
      │   │           ├─ 重复上述指标计算和合并
      │   │           └─ 建立父子关系: set_klu_parent_relation()
      │   │
      │   └─ 返回所有级别的 K 线数据
      │
      └─ [批量模式] cal_seg_and_zs() for 每个级别
          ├─ bi_list.try_add_virtual_bi() → 补全虚笔
          ├─ cal_seg() → seg_list.update() → CSegListChan.cal_seg_sure()
          ├─ zs_list.cal_bi_zs() → 中枢计算
          ├─ update_zs_in_seg() → 中枢分配到线段
          ├─ segseg_list (线段的线段)
          ├─ segzs_list (线段中枢)
          └─ bs_point_lst.cal() → 买卖点计算

3. 绘图
   └─ CPlotDriver(chan, plot_config, plot_para)
```

### 5.2 步进模式完整流程

```
1. 创建 CChan(config={'trigger_step': True})
   └─ __init__ 中不立即计算（因为 trigger_step=True）

2. 遍历 chan.step_load()
   └─ do_init() 清空数据
   └─ 每根 K 线:
       ├─ load_iterator(step=True) → 每来一根 K 线就 yield self
       ├─ add_single_klu()
       │   └─ 如果 bi_list.update_bi() 返回 True（有新笔）
       │       └─ cal_seg_and_zs() 立即重算段/中枢/买卖点
       └─ yield chan_snapshot

3. 每帧获取买卖点
   └─ chan_snapshot.get_latest_bsp() → 判断是否交易
```

### 5.3 外部喂 K 线流程（实盘）

```
1. 创建 CChan(不设 begin_time, end_time)
2. 每收到新 K 线数据:
   └─ chan.trigger_load({KL_TYPE.K_5M: [new_klu1, new_klu2], ...})
       └─ 增量更新所有缠论元素
       └─ cal_seg_and_zs()
```

---

## 第六部分：实战示例

### 6.1 基本使用

```python
from Chan import CChan
from ChanConfig import CChanConfig
from Common.CEnum import DATA_SRC, KL_TYPE, AUTYPE
from Plot.PlotDriver import CPlotDriver

# 1. 配置
config = CChanConfig({
    "bi_strict": True,
    "trigger_step": False,
    "divergence_rate": 0.9,
    "min_zs_cnt": 1,
})

# 2. 创建 CChan 实例
chan = CChan(
    code="sz.000001",              # 股票代码
    begin_time="2020-01-01",       # 开始时间
    end_time=None,                 # 结束时间(None=最新)
    data_src=DATA_SRC.BAO_STOCK,   # 数据源
    lv_list=[KL_TYPE.K_DAY],       # 级别列表
    config=config,
    autype=AUTYPE.QFQ,             # 前复权
)

# 3. 获取结果
day_data = chan[0]  # 或 chan[KL_TYPE.K_DAY]
bi_list = day_data.bi_list        # 笔列表
seg_list = day_data.seg_list      # 线段列表
zs_list = day_data.zs_list        # 中枢列表
bsp_list = day_data.bs_point_lst  # 买卖点列表

# 4. 遍历笔
for bi in bi_list:
    print(f"笔{bi.idx}: {bi.dir} {bi.get_begin_val()} -> {bi.get_end_val()} sure={bi.is_sure}")

# 5. 绘图
plot_config = {
    "plot_kline": True,
    "plot_bi": True,
    "plot_seg": True,
    "plot_zs": True,
    "plot_bsp": True,
}
driver = CPlotDriver(chan, plot_config=plot_config)
driver.figure.show()
driver.save2img("./result.png")
```

### 6.2 策略回测示例

```python
from Chan import CChan
from ChanConfig import CChanConfig
from Common.CEnum import BSP_TYPE, FX_TYPE, DATA_SRC, KL_TYPE, AUTYPE

config = CChanConfig({
    "trigger_step": True,  # 开启步进模式！
    "divergence_rate": 0.8,
    "min_zs_cnt": 1,
})

chan = CChan(
    code="sz.000001",
    begin_time="2021-01-01",
    data_src=DATA_SRC.BAO_STOCK,
    lv_list=[KL_TYPE.K_DAY],
    config=config,
    autype=AUTYPE.QFQ,
)

is_hold = False
last_buy_price = None

for chan_snapshot in chan.step_load():
    bsp_list = chan_snapshot.get_latest_bsp()
    if not bsp_list:
        continue

    last_bsp = bsp_list[0]
    cur_lv_chan = chan_snapshot[0]

    # 判断是否是一类买点且底分型刚形成
    if (BSP_TYPE.T1 in last_bsp.type or BSP_TYPE.T1P in last_bsp.type) \
       and cur_lv_chan[-2].fx == FX_TYPE.BOTTOM \
       and last_bsp.is_buy and not is_hold:
        last_buy_price = cur_lv_chan[-1][-1].close
        print(f"买入: {cur_lv_chan[-1][-1].time} 价格={last_buy_price}")
        is_hold = True

    # 一类卖点平仓
    elif (BSP_TYPE.T1 in last_bsp.type) \
         and cur_lv_chan[-2].fx == FX_TYPE.TOP \
         and not last_bsp.is_buy and is_hold:
        sell_price = cur_lv_chan[-1][-1].close
        profit = (sell_price - last_buy_price) / last_buy_price * 100
        print(f"卖出: {cur_lv_chan[-1][-1].time} 价格={sell_price} 收益率={profit:.2f}%")
        is_hold = False
```

### 6.3 自定义数据源

```python
from DataAPI.CommonStockAPI import CCommonStockApi
from Common.CEnum import DATA_FIELD
from Common.CTime import CTime
from KLine.KLine_Unit import CKLine_Unit

class CMyDataAPI(CCommonStockApi):
    def __init__(self, code, k_type, begin_date, end_date, autype):
        super().__init__(code, k_type, begin_date, end_date, autype)

    def get_kl_data(self):
        # 从你的数据源读取 K 线数据
        for row in your_data_source:
            yield CKLine_Unit({
                DATA_FIELD.FIELD_TIME: CTime(row.year, row.month, row.day, 0, 0),
                DATA_FIELD.FIELD_OPEN: row.open,
                DATA_FIELD.FIELD_CLOSE: row.close,
                DATA_FIELD.FIELD_HIGH: row.high,
                DATA_FIELD.FIELD_LOW: row.low,
            })

    def SetBasciInfo(self):
        self.name = "我的股票"

    @classmethod
    def do_init(cls):
        pass  # 无需初始化

    @classmethod
    def do_close(cls):
        pass  # 无需关闭

# 使用自定义数据源
chan = CChan(
    code="mycode",
    data_src="custom:MyDataModule.CMyDataAPI",
    lv_list=[KL_TYPE.K_DAY],
)
```

---

## 附录：关键术语对照表

| 中文 | 英文/代码中 | 说明 |
|------|-----------|------|
| K线 | KLine / KL | 蜡烛图 |
| 单根K线 | KLine_Unit / klu | 未合并的原始 K 线 |
| 合并K线 | CKLine / klc | 包含处理后的 K 线 |
| 分形/分型 | FX | 顶分型/底分型 |
| 笔 | Bi | 连接顶底分型的基本走势单元 |
| 虚笔 | virtual bi | 尚未确定的笔 |
| 线段 | Seg | 由至少 3 笔组成的走势 |
| 虚段 | virtual seg | 尚未确定的线段 |
| 中枢 | ZS / zhongshu | 至少 3 笔重叠的价格区间 |
| 买卖点 | BSP | 形态学买卖点 |
| 一类买卖点 | T1 | 趋势背驰产生的买卖点 |
| 二类买卖点 | T2 | 一类后的回撤买卖点 |
| 三类买卖点 | T3A/T3B | 中枢后回抽确认点 |
| 背驰 | divergence | 价格创新高/低但 MACD 力度减弱 |
| 特征序列 | eigen | 线段中反向笔构成的序列 |
| 区间套 | qjt | 多级别联立分析 |
| 级别 | lv / KL_TYPE | K 线时间周期 |
| 包含处理 | combine | K 线合并处理 |
| 前复权 | QFQ | 向前复权 |
| 后复权 | HFQ | 向后复权 |

---

> **参考文档**：
> - [原始仓库 README.md](https://github.com/Vespa314/chan.py)
> - [快速上手指南 quick_guide.md](quick_guide.md)
> - [B 站交流会视频](https://www.bilibili.com/video/BV1nu411c7oG/)